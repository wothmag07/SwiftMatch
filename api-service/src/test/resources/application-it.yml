spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/swiftmatch
    username: swiftmatch
    password: swiftmatch
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true
    clean-disabled: false
